import {Platform} from 'react-native'
export const trainingDev = {
    headname: 'Training & Development',
    icon: Platform === 'android' ? 'md-ribbon': 'ios-ribbon',
    list:
    [
        'Java',
        'Blockchain'
    ]
}